# Sub2API v0.2.8 人工裁决登记

上游固定为 d7a82d78ca51d42be41cb4daa3510ea401defe9f / fd80b08c90b55edcad5b00171b53f08721d30da1。真实共同祖先 efe9aab1e4ec89a42ba45e8dac20e882c5409a6a。首次 prepare 拒绝清单外冲突并中止，现逐项登记人工裁决责任，不放宽通配范围或审计要求。

- 发布 workflow、独立产品版本、后台导出与对话框焦点栈保留 XY2API；吸收与之兼容的上游补充。
- 设置/DTO/依赖注入合并 STATE/IQ/质量路由与 Claude 客户端版本、OpenCode 用量的独立字段和生命周期。
- 账号更新保留 current_extra、IQ 行锁和私有 STATE 过滤，同时合入 OpenCode 同 Key 用量身份保护。
- HTTP 保留禁止 POST 重放约束，接入上游取消响应与 HTTP/2 修复；Gemini countTokens 保留二开正文处理和上游 failover。
- 计费保留按分组长上下文开关，整套引入推理倍率及最终力度传播。
- 代理过期保留账号锁及票据失效，同时复核快照以免覆盖并发续期。
- 插件 v1 协议只追加结构化只读元数据；保持已有字段编号和魔术字，生成文件重新生成。
- 新迁移按只追加要求映射为 253/254/255，历史 299 个 SQL 和 checksum 保持不变。

具体逐文件结果与测试将在 provenance/report 中完成，所有 pending 必须清零后才能合并。
