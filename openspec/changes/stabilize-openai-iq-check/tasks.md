# 实施任务

实施已落到标准源码目录。勾选表示已完成并有本地证据；真实上游逐组合验收单独列出，不冒充模拟测试。

## 已完成的规划

- [x] 核实 `main` / `dff987fb16`、现有检测链路、仓库规范及用户最新需求。
- [x] 实际执行当前解析器的边界输入，记录歧义、反例、重复键与精度误判。
- [x] 核实模型列表默认回退和同步接口写账号元数据的行为。
- [x] 核对 OpenAI 官方结构化输出与 Astra 思考深度文档。
- [x] 形成配置、模型发现、判分、状态迁移、历史和测试规范。

## 第一批：判分与请求契约

- [x] 先将 design 中已复现问题转为失败回归，覆盖完整正确/错误/未知边界。
- [x] 引入精确答案提取，处理重复键、否定、反例、更正和多候选；记录提取原因。
- [x] 统一 `candy-v2` 提示与 JSON Schema；验证发送内容中不注入标准答案。
- [x] Payload 使用任务配置，正确区分 Responses/Chat 和 `upstream_default` 参数省略。
- [x] 流式解析补齐重复/冲突终结、拒答、超限及中途错误用例；不从推理提取答案。

## 第二批：设置、任务与数据

- [x] 可选配置字段与默认化；旧客户端、列表开关和批量的“未修改”语义。
- [x] 追加迁移 241；沿用现有 JSON 域和原始 SQL 记录表，无 Ent/Wire 结构或依赖变更，不需生成变动；记录实际参数和判题版本，保留旧记录原义。
- [x] 修正间隔修改；模型/深度/协议等变更失效旧任务，正常刷新不失效。
- [x] 任务快照、事务提交、过期恢复、抖动与跨实例并发；保持现有独立调度限制。
- [x] 保持每账号最近两轮，包括第三轮运行中、失败、取消、删除和崩溃恢复。

## 第三批：模型发现与管理界面

- [x] 提取只读目录功能，新增管理员 IQ 模型目录接口；不改账号白名单/元数据。
- [x] 上游 ID 与逐字段能力来源、5分钟缓存、刷新去重、错误和过期标记。
- [x] 检测间隔旁增加可输入的模型单选、拉取按钮、动态思考深度及上游默认选项。
- [x] 编辑、批量、开关及旧设置初始化；切换账号/模型的异步竞态处理。
- [x] 不完整能力、自定义模型、无推理模型、未保存凭据变更的交互与校验。
- [x] 记录显示规范化答案和本轮模型/深度；原始回答可展开，错误原因翻译。
- [x] 桌面、小屏、键盘操作和中英文文案检查。

## 验收门禁

- [x] 判分、请求构造与 service 单测；相同输入重复判分一致。
- [x] 模拟 OAuth Responses、API key Responses、API key Chat；拉取失败与自定义模型均可配置。
- [x] PostgreSQL 集成：只改间隔、改模型/深度时旧任务回写、关闭/重开、令牌刷新、两轮清理、进程重启和多实例竞争。
- [x] 验证模型拉取不修改账号、分组、白名单或 IQ 状态；回退参考目录不能标成该账号实际支持。
- [x] 检查每个路由与缓存/粘性候选都保留人工限制且不能绕过生效 IQ 限制。
- [x] 前端相关测试、lint、类型、国际化与构建；Chromium 在 1280px / 390px 验证三列控件布局和交互。构建首次退出137，串行限制内存重跑通过。
- [x] 明确兼容性范围：三种协议路径已模拟验证；未使用真实账号逐模型调用，真实组合标记未验证，不宣称全上游支持。
- [x] 更新使用文档和发行说明，明确“无法解析改为未知”及模型/深度可配。
- [x] 重用四角色交付与同输入 BASELINE/MODIFIED/ROLLBACK；补丁重建、源码回滚哈希、数据库追加兼容及事务回滚通过。生产回滚流程已文档化，未操作生产数据。

## 不在本次默认实现内

连续多次确认、投票、自动降深度/换模型重试、未知时保持历史降智封锁、强制使用独立开关。用户以后明确选择时再另建规格，不在稳定性修复中隐式加入。
