# 实施任务

用户已批准实施。代码位于 `feat/iq-monitoring-controls`；下列勾选区分实现完成与验证完成。

- [x] 阅读用户给定公告的官方联合发布镜像，区分防御建议与OpenAI生产事实。
- [x] 核对模型、结构化输出、推理预算、限流、数据控制与社区体验报告。
- [x] 审查当前Payload/Grade、IQService、Claim/Complete、并发服务、诊断和UI。
- [x] 写出明确评分不变量、固定/节约模式、失败分类、暂停、预算及验收场景。
- [x] 在独立实现分支整合现有OAuth协议修复及迁移242，补齐前一轮未完成的全service/repository静态检查。
- [x] 提取端点请求配置，修正普通API key的Codex专用头使用；保留真实认证与协议所需字段。
- [x] 扩展有界error.code/type、Retry-After、usage；覆盖HTTP和SSE错误。
- [x] 新增配置/执行状态/预算字段，明确旧账号默认值、只改间隔与模型变更的不同语义。
- [x] 将pending与start分离，接入业务并发名额、预算预留、实际尝试计数和超时/续租。
- [x] 实现固定模式门禁、手动去重、健康暂停及按原因退避；先保留固定间隔验收。
- [x] 实现显式adaptive模式及新鲜度；连续smart仅影响间隔，三态判分仍单次。
- [x] 更新设置/批量/列表/记录和诊断下载接口，省略字段不覆盖，缓存和分页一致。
- [x] 追加迁移243及checksum；重新生成Wire，Ent结构保持原JSON字段，历史迁移逐字节不变。
- [x] 以注入时间验证排期、并发竞争、日预算、日期切换、Retry-After和重启；完整PostgreSQL/Redis仓储集成验证事务和两轮保留。
- [x] 运行相关后端套件、全变更静态检查、前端测试/lint/类型/i18n/构建与移动端验证。
- [x] 用同一上游合成输入完成实施版BASELINE/MODIFIED/ROLLBACK，不把当前方案文档验证当作实现验收。
- [ ] 后续单独授权后再选择少量真实账号观察实际周期；不重复采样直到正确，不绕过拒绝，不承诺误分类率已证明下降。

交付记录沿用`/xy/artifacts/openai-iq-check/VERIFICATION.txt`。本轮实现未提交、推送、发版或部署；真实账号观察属于后续独立操作，不用模拟结果证明服务方误分类率。
