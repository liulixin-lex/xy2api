# 本次会话最终审查

目标为本地代码升级；本轮没有连接生产。真实流量表现不在本次本地验收的结论内。PR31已合并，本地改进继续保存在fix/iq-detection-operations；原main保持干净。

## 已发现并修复的问题

| 问题 | 触发与影响 | 最终处理 |
| --- | --- | --- |
| 有空位仍反复繁忙 | 上限2、占用1，旧策略额外预留一格，至少三次等待 | 直接竞争原上限内任何空闲名额 |
| 瞬时满载立即延期 | 业务请求短暂占满，检测错过马上释放的空位 | 同一领取最多4次原子获取，250/500/1000ms有界等待；持续满载才延期一次 |
| 短冷却被扩大 | 剩5秒冷却却返回一分钟 | 按实际最晚冷却时间恢复，仍受15秒扫描粒度影响 |
| 业务额度竞争窗口 | 领取后业务额度耗尽，开始事务仍扣检测预算并启动 | 行锁内复核总/日/周业务额度，拒绝启动不占账号/组预算、不新增历史 |
| 较短延期覆盖较长限制 | 一小时冷却被组暂停的一分钟或繁忙20秒覆盖 | Start/Defer保留最晚约束，减少反复唤醒与误导时间 |
| 批量重置保留旧繁忙计数 | 换模型后仍保留此前3次繁忙，单账号修改则归零 | 追加244迁移，仅替换设置函数；更换配置归零，间隔修改保留计数 |
| 弹窗键盘和卸载问题 | 关闭后焦点丢失、Tab离开弹窗、嵌套标题ID重复 | 唯一标题ID、Tab循环、卸载恢复焦点；嵌套Escape及滚动锁单独管理 |

## 需求与验证对应

- PR31判分/解析：保留歧义答案、重复关键字段和冲突最终消息保护；相关domain、iqcheck、service测试已复验。
- 并发：服务测试覆盖有空位、最后空位、满载、瞬时冲突、Redis异常、无限制以及取消等待；真正准入沿用共享Redis原子脚本，不增加账号上限。
- 租约/预算：PostgreSQL18.1与Redis8.4本地集成覆盖领取、跨副本、重启恢复、旧revision、过期租约、重复开始、共享预算及新增竞争场景，16项主测试通过。
- 前端：账号列表32px单行摘要；详细执行原因、最近/下次时间、预算与三轮历史集中到记录页，关闭停止轮询。98项相关测试通过，新增滚动锁检查后的14项重叠测试亦通过。
- 浏览器：Chromium合成数据、1280px浅色及390px深色；无横向溢出，刷新失败保留三轮历史，Escape/焦点返回、24步Tab循环、停止轮询及诊断文件落盘解析通过。首次脚本在异步弹窗加载完成前读取DOM失败，改为等待就绪后通过，不算应用失败或首次验收成功。
- 迁移：只追加244；旧迁移目录311个文件已与修改前归档逐一核对，字节完全一致（包含迁移测试与支持文件）。源码回滚只作用于临时副本。
- 对照：本轮额度/冷却/批量/键盘问题均实际完成旧逻辑失败、修复通过、回滚再次失败，完整恢复副本3944个文件字节与原归档一致。减少延期策略的独立对照与最终构建由交付目录final-audit-finish-checks.mjs完成，逐条命令、退出码及输出见VERIFICATION.txt。

## 最终执行结果

2026-09-15 08:38:05 UTC，顺序门禁全部结束：最新7项服务监测主测试、最终前端构建、服务层综合静态检查（exit0、0 issues）及嵌入前端的服务编译均成功。减少延期的相同输入对照为BASELINE exit1、MODIFIED exit0、ROLLBACK exit1；两个exit1为旧缺陷复现。具体断言分别验证最后空闲槽位直接执行、短暂满载两次后约0.75秒只发送一次检测请求、5秒健康冷却不扩大到一分钟。

实现继续保存在本地fix/iq-detection-operations，尚未提交、推送或部署。四角色归档的补丁重建、回滚和重读由final-audit-closeout.mjs及final-audit-finalize.mjs记录；最终文件树哈希以site-review-final-result.json为准。

## 实际限制

满载、上游Retry-After、人工停用、账号和组预算不能跳过；内部重试只争取并发名额，不重复发送模型检测请求。1.75秒为等待定时器总量，不含Redis调用耗时；15秒后台扫描仍会增加排期误差。计数记录延期事件，内部四次获取仅算一次延期。

账号列表主模块仍有体积提示，属于既有大页面结构；本次仅将记录弹窗拆分加载。旧Browserslist数据提示和已有构建警告保留，不为此升级整站依赖。并行验证触发本地内存压力，构建退出137、Go检查主动SIGTERM的记录保留，最终改为顺序验证，不能把中断当通过。

生产曾发生的误部署事件及停止线上操作的约束完整保留在PROJECT_MEMORY；禁止重跑任何历史部署/恢复脚本。新版未进行真实账号调用或生产部署，因此本地测试通过不能表述为生产问题已消失。
